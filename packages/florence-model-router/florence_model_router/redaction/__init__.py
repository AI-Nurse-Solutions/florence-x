"""PHI redaction pipeline (runs before any external model call). Phase 4."""
from __future__ import annotations


def redact(text: str) -> str:  # pragma: no cover - Phase 4 stub
    """Placeholder. Phase 4 implements clinical NER + tokenization (HMAC-SHA256)."""
    raise NotImplementedError("Redaction pipeline is implemented in Phase 4.")
