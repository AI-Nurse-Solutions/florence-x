[project]
name = "florence-core"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = ["pydantic>=2,<3", "pyyaml>=6"]
[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
[tool.hatch.build.targets.wheel]
packages = ["florence_core"]
