[project]
name = "florence-connectors"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = ["florence-core"]
[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
[tool.hatch.build.targets.wheel]
packages = ["florence_connectors"]
